name: Swiftlint Check

on: 
  pull_request

jobs:
  swifltint:
    runs-on: macos-latest
    steps:
    - name: Repository checkout
      uses: actions/checkout@v2
    - name: Run SwiftLint
      run: swiftlint